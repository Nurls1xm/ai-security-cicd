/**
 * DANGEROUS CODE - JavaScript vulnerabilities.
 * 
 * This file contains dangerous JavaScript patterns.
 * AI should BLOCK deployment.
 */

// ATTACK 1: eval() - Arbitrary Code Execution
function dangerousEval(userInput) {
    const result = eval(userInput);
    console.log("DANGEROUS: eval() detected!");
    return result;
}

// ATTACK 2: innerHTML - XSS Vulnerability
function dangerousInnerHTML(userContent) {
    document.getElementById('content').innerHTML = userContent;
    console.log("DANGEROUS: innerHTML detected!");
}

// ATTACK 3: exec() - Command Execution (Node.js)
const { exec } = require('child_process');

function dangerousExec(command) {
    exec(command, (error, stdout, stderr) => {
        console.log("DANGEROUS: exec() detected!");
        console.log(stdout);
    });
}

// ATTACK 4: Function constructor - Code Injection
function dangerousFunctionConstructor(code) {
    const fn = new Function(code);
    fn();
    console.log("DANGEROUS: Function constructor detected!");
}

// ATTACK 5: setTimeout with string - Code Execution
function dangerousSetTimeout(code) {
    setTimeout(code, 1000);
    console.log("DANGEROUS: setTimeout with string detected!");
}

console.log("⚠️ This JavaScript code contains DANGEROUS patterns!");
console.log("❌ AI should BLOCK this deployment");
